const initialState = {
    friendsList : [],
    isLoading: [],
    error:'',
    fireRedirect: false,
}
export const reducer = (state = initialState, action) => {
    switch(action.type){
        default:
            return state;
    }
}